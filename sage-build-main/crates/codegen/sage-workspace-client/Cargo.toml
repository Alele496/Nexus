[package]
license = "Apache-2.0"
name = "sage-workspace-client"
version = "0.1.0"
edition.workspace = true
description = "Lightweight typed client for hub-proxied workspace.* RPCs (shared by sage-shell proxy mode and other consumers)"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["time"] }
tracing = { workspace = true }
sage-computer-hub-sdk = { workspace = true }
sage-workspace-types = { workspace = true }
sage-tool-protocol = { workspace = true }
sage-tool-runtime = { workspace = true }

[features]
default = []
default-bazel = []
[dev-dependencies]
schemars = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt", "test-util"] }
sage-tool-types = { workspace = true }

[lints]
workspace = true
